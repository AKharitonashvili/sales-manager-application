/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LatinLettersOnlyDirective } from './latin-letters-only.directive';

describe('Directive: LatinLettersOnly', () => {
  it('should create an instance', () => {
    const directive = new LatinLettersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
